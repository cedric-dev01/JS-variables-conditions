const prix = 90000
const surface = 70
const nbpieces = 3

if(prix === 90000 && surface === 70 && nbpieces === 3){
	console.log("Les trois variables sont identiques");
}

if(prix === 90000 || surface === 70 || nbpieces === 3){
	console.log("Deux des variables sont identiques");
}

else {
  console.log("toutes les variables sont différentes");
}
